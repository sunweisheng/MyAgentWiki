# 全链路规则与 LLM 协同判定设计

## 文档定位

本文档不再承担主详细设计文档的职责。

当前主线设计、处理流程、证据链、语义链、审核恢复链、阅读包和回答交接契约，统一以 [MyAgentWiki系统详细设计.md](/Users/sunweisheng/Documents/GitHub/MyAgentWiki/docs/MyAgentWiki系统详细设计.md) 为准。

本文档保留为专题设计稿，主要回答一个更聚焦的问题：

“在证据优先的整体架构下，LLM 语义判定究竟应该介入哪些阶段、承担哪些职责、遵守哪些边界？”

## 适用范围

当你主要关心下面这些问题时，再读本文档：

- 为什么不能只在概念页生成末端补几个标题过滤规则
- 为什么要把 `document analysis / claim role / page intent` 拆成正式语义分析阶段
- 为什么语义决策必须独立成账本
- 为什么 LLM 要优先批处理，而不是单条高频调用
- 为什么 grounded 改写只能发生在证据和页型已经确定之后

如果你想先理解整个系统，请先读主文档。

## 与主文档的关系

当前关系固定如下：

- 主文档负责“系统全貌和端到端流程”
- 本文档负责“LLM 协同判定这一专题的设计理由和细化原则”

因此，本文件不再重复展开以下内容：

- 全部目录设计
- 全部数据模型字段
- 全部 query / review / lint / testing 细节
- 全部历史版本边界说明

这些内容若后续需要变更，应先改主文档，再决定是否同步补充到本专题稿。

## 核心判断

### 1. 问题不是“概念页标题不好”，而是语义职责分层不清

如果只在概念页末端补标题过滤规则，系统仍然会不断遇到同类问题：

- 结构标题被当成主题候选
- 步骤标题被当成概念
- 总结段、建议句、示例名误生成正式页面
- 明明应该在 Claim 层解决的问题，被拖到页面层兜底

因此，问题的根源不是“命名策略不够聪明”，而是“语义职责没有前移并显式建模”。

### 2. LLM 的最佳角色是受限语义分析器

LLM 不应作为事实来源，也不应作为任意文本重写器。

更合理的角色是：

- 对脚本难以稳定处理的灰区做结构化判定
- 输出有限枚举、有限字段、有限动作
- 所有结果都能回链到输入对象

这意味着 LLM 最适合做的是：

- 文档结构判定
- Claim 角色判定
- 页面意图判定
- 灰区候选的 accept / reject / rename / reroute
- grounded 的可读化改写

### 3. 语义分析应该作为正式阶段，而不是零散 hook

如果把 LLM 只当作若干 hook，系统会长期面临几个问题：

- 同一类判断散落在多个命令里
- 不同命令对同一问题使用不同话语体系
- prompt 升级、schema 升级和缓存失效没有统一落点
- 后续很难解释“为什么这条路由结果和另一条不一样”

因此，语义协同应被正式建模为若干语义分析阶段，而不是零散外挂点。

## 建议保留的专题原则

### 确定性优先，按需进入语义灰区

- 能用脚本稳定解决的问题，不交给 LLM
- 只有在字符串规则不足以可靠判定语义角色时，才进入 LLM 灰区
- 不能因为 LLM 看起来更灵活，就把本可确定的事情交给模型

### 先判页型，再命名，再渲染

正确顺序应为：

1. 先判断内容属于什么语义角色
2. 再判断是否值得成页，以及适合成什么页
3. 最后才允许命名和可读化改写

这条顺序的价值，在于把“生成正式页面”的门槛前移，避免大量噪声候选进入后半段。

### 批处理优先于单例调用

- 默认优先批量调用 LLM
- 以同一任务类型、同一阶段、同一上下文模板为单位组织批次
- 输出必须满足严格 JSON schema

这样做的原因是：

- 减少静态上下文重复传输
- 便于缓存、重试和预算管理
- 便于后续统一审计与回放

### 语义账本与证据账本必须分离

证据层负责回答“材料里有什么”。

语义层负责回答“系统为什么这样理解和组织这些材料”。

如果把所有可变语义字段直接塞回确定性账本，系统会失去下面这些能力：

- 重跑语义层而不污染证据层
- 比较不同 prompt / 模型 / schema 的判断差异
- 给 lint、review、page rebuild 提供可解释链路

### grounded 输出是硬约束，不是风格偏好

LLM 只能在已有 `normalized / chunks / claims / pages / reviews` 证据上做：

- 分类
- 选择
- 重命名
- 汇总
- 可读化表达

无法 grounded 的结果必须：

- 回退到脚本默认行为
- `abstain`
- 或升级人工判断

## 与当前实现最相关的专题结论

### 1. `document analysis / claim role / page intent` 应视为同一条语义主链

不要把它们理解为互不相关的局部优化。

这三者共同决定：

- 文档怎么切
- Claim 怎么分类
- 页面怎么路由

如果前两步没收口，第三步再聪明也会不断被噪声拖累。

### 2. `review-auto` 和 `stable promotion` 本质上也属于语义协同链的一部分

它们虽然更靠近审核和恢复层，但本质上仍然是在处理：

- “哪些对象已足够明确，可以保守自动收口”
- “哪些对象仍然需要人做最终判断”

所以它们不应被看成和语义判定完全无关的独立小工具。

### 3. 页面可读化改写必须晚于页型收口

否则系统会在还没确定对象是否适合成页时，就先做表达润色，最后把表达层误当成结构正确。

顺序必须固定为：

- 先有证据
- 再有语义判断
- 再有页面路由
- 最后才有 grounded rewrite

## 后续维护建议

若后续需要继续完善本专题稿，建议只沿这几个方向补充：

- 各类语义分析阶段的输入 schema 与输出 schema
- 批调度器如何分桶、分片、缓存和重试
- `abstain` 与 fallback 的标准协议
- 语义决策如何参与 lint、review 和 page rebuild

如果要修改系统主流程、主术语、主数据模型或主边界，请优先更新主文档，而不是先改这里。
